[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "contoso-foundry-agent-lab"
version = "0.1.0"
description = "Pro-code skeleton for the Contoso Foundry Agent Lab using Foundry IQ and Foundry tools"
requires-python = ">=3.11"
dependencies = [
  "agent-framework",
  "agent-framework-foundry",
  "azure-identity",
  "python-dotenv",
  "pydantic>=2",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["src/tests"]
pythonpath = ["src"]
